---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: dags.kontroler.greedykomodo
spec:
  group: kontroler.greedykomodo
  names:
    kind: DAG
    listKind: DAGList
    plural: dags
    singular: dag
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DAG is the Schema for the dags API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DAGSpec defines the desired state of DAG
            properties:
              dsl:
                description: DSL string to define the DAG using the DSL syntax When
                  provided, this takes precedence over the individual fields above
                type: string
              parameters:
                items:
                  properties:
                    defaultFromSecret:
                      type: string
                    defaultValue:
                      type: string
                    name:
                      type: string
                  required:
                  - name
                  type: object
                type: array
              schedule:
                type: string
              suspended:
                type: boolean
              task:
                items:
                  description: TaskSpec defines the structure of a task in the DAG
                  properties:
                    args:
                      items:
                        type: string
                      type: array
                    backoff:
                      description: Backoff defines the backoff strategy for a task
                      properties:
                        limit:
                          type: integer
                      required:
                      - limit
                      type: object
                    command:
                      items:
                        type: string
                      type: array
                    conditional:
                      description: Conditional defines the conditional execution parameters
                      properties:
                        enabled:
                          type: boolean
                        retryCodes:
                          items:
                            type: integer
                          type: array
                      required:
                      - enabled
                      - retryCodes
                      type: object
                    image:
                      type: string
                    name:
                      type: string
                    parameters:
                      items:
                        type: string
                      type: array
                    podTemplate:
                      description: PodTemplateSpec defines the template for the pod
                        of a task
                      properties:
                        activeDeadlineSeconds:
                          description: ActiveDeadlineSeconds is how long the pod will
                            last for (basically a time-limit) Will Start the moment
                            the pod is scheduled for a node, will count down even
                            if in pending state
                          format: int64
                          type: integer
                        affinity:
                          $ref: '#/definitions/k8s.io~1api~1core~1v1~0Affinity'
                        automountServiceAccountToken:
                          type: boolean
                        imagePullSecrets:
                          items:
                            $ref: '#/definitions/k8s.io~1api~1core~1v1~0LocalObjectReference'
                          type: array
                        nodeSelector:
                          additionalProperties:
                            type: string
                          type: object
                        resources:
                          $ref: '#/definitions/k8s.io~1api~1core~1v1~0ResourceRequirements'
                        securityContext:
                          $ref: '#/definitions/k8s.io~1api~1core~1v1~0PodSecurityContext'
                        serviceAccountName:
                          type: string
                        tolerations:
                          items:
                            $ref: '#/definitions/k8s.io~1api~1core~1v1~0Toleration'
                          type: array
                        volumeMounts:
                          items:
                            $ref: '#/definitions/k8s.io~1api~1core~1v1~0VolumeMount'
                          type: array
                        volumes:
                          items:
                            $ref: '#/definitions/k8s.io~1api~1core~1v1~0Volume'
                          type: array
                      type: object
                    runAfter:
                      items:
                        type: string
                      type: array
                    script:
                      type: string
                    scriptInjectorImage:
                      description: Used to select the image that is used to push to
                        script into the pod
                      type: string
                    taskRef:
                      description: Using reference to existing pre-created task -
                        cannot reference another in-line task
                      properties:
                        name:
                          type: string
                        version:
                          type: integer
                      required:
                      - name
                      - version
                      type: object
                  required:
                  - name
                  type: object
                type: array
              webhook:
                properties:
                  url:
                    type: string
                  verifySSL:
                    type: boolean
                required:
                - url
                - verifySSL
                type: object
              workspace:
                properties:
                  enable:
                    type: boolean
                  pvc:
                    properties:
                      accessModes:
                        items:
                          $ref: '#/definitions/k8s.io~1api~1core~1v1~0PersistentVolumeAccessMode'
                        type: array
                      resources:
                        $ref: '#/definitions/k8s.io~1api~1core~1v1~0VolumeResourceRequirements'
                      selector:
                        description: A label selector is a label query over a set
                          of resources. The result of matchLabels and matchExpressions
                          are ANDed. An empty label selector matches all objects.
                          A null label selector matches no objects.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: A label selector requirement is a selector
                                that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: operator represents a key's relationship
                                    to a set of values. Valid operators are In, NotIn,
                                    Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: values is an array of string values.
                                    If the operator is In or NotIn, the values array
                                    must be non-empty. If the operator is Exists or
                                    DoesNotExist, the values array must be empty.
                                    This array is replaced during a strategic merge
                                    patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: matchLabels is a map of {key,value} pairs.
                              A single {key,value} in the matchLabels map is equivalent
                              to an element of matchExpressions, whose key field is
                              "key", the operator is "In", and the values array contains
                              only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                      storageClassName:
                        type: string
                      volumeMode:
                        $ref: '#/definitions/k8s.io~1api~1core~1v1~0PersistentVolumeMode'
                    required:
                    - accessModes
                    type: object
                required:
                - enable
                - pvc
                type: object
            type: object
          status:
            description: DAGStatus defines the observed state of DAG
            properties:
              message:
                description: reason for the current phase
                type: string
              phase:
                description: shows the current phase of the DAG
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
