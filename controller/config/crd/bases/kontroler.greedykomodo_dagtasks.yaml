---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: dagtasks.kontroler.greedykomodo
spec:
  group: kontroler.greedykomodo
  names:
    kind: DagTask
    listKind: DagTaskList
    plural: dagtasks
    singular: dagtask
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DagTask is the Schema for the dagtasks API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DagTaskSpec defines the desired state of DagTask
            properties:
              args:
                items:
                  type: string
                type: array
              backoff:
                description: Backoff defines the backoff strategy for a task
                properties:
                  limit:
                    type: integer
                required:
                - limit
                type: object
              command:
                items:
                  type: string
                type: array
              conditional:
                description: Conditional defines the conditional execution parameters
                properties:
                  enabled:
                    type: boolean
                  retryCodes:
                    items:
                      type: integer
                    type: array
                required:
                - enabled
                - retryCodes
                type: object
              image:
                type: string
              parameters:
                items:
                  type: string
                type: array
              podTemplate:
                description: 'PodTemplateSpec defines the template for the pod of
                  a task Note: use CRD-safe local types to avoid controller-gen emitting
                  $ref to core/v1 and causing forbidden $ref entries in the CRD schema.'
                properties:
                  activeDeadlineSeconds:
                    description: ActiveDeadlineSeconds is how long the pod will last
                      for (basically a time-limit) Will Start the moment the pod is
                      scheduled for a node, will count down even if in pending state
                    format: int64
                    type: integer
                  affinity:
                    properties:
                      nodeAffinity:
                        description: keep opaque for brevity; map to corev1.Affinity
                          in conversion
                        x-kubernetes-preserve-unknown-fields: true
                      podAffinity:
                        x-kubernetes-preserve-unknown-fields: true
                      podAntiAffinity:
                        x-kubernetes-preserve-unknown-fields: true
                    type: object
                  automountServiceAccountToken:
                    type: boolean
                  imagePullSecrets:
                    items:
                      properties:
                        name:
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  nodeSelector:
                    additionalProperties:
                      type: string
                    type: object
                  resources:
                    properties:
                      limits:
                        additionalProperties:
                          type: string
                        description: 'simplified: requests/limits maps'
                        type: object
                      requests:
                        additionalProperties:
                          type: string
                        type: object
                    type: object
                  securityContext:
                    properties:
                      fsGroup:
                        description: minimal mapping — expand if needed
                        format: int64
                        type: integer
                    type: object
                  serviceAccountName:
                    type: string
                  tolerations:
                    items:
                      properties:
                        effect:
                          type: string
                        key:
                          type: string
                        operator:
                          type: string
                        tolerationSeconds:
                          format: int64
                          type: integer
                        value:
                          type: string
                      type: object
                    type: array
                  volumeMounts:
                    items:
                      properties:
                        mountPath:
                          type: string
                        name:
                          type: string
                        readOnly:
                          type: boolean
                      required:
                      - mountPath
                      - name
                      type: object
                    type: array
                  volumes:
                    items:
                      properties:
                        emptyDir:
                          description: Only support EmptyDir and PersistentVolumeClaim
                            for now
                          type: object
                        name:
                          type: string
                        persistentVolumeClaim:
                          properties:
                            claimName:
                              type: string
                          required:
                          - claimName
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                type: object
              runAfter:
                items:
                  type: string
                type: array
              script:
                type: string
              scriptInjectorImage:
                description: Used to select the image that is used to push to script
                  into the pod
                type: string
            required:
            - backoff
            - conditional
            - image
            type: object
          status:
            description: DagTaskStatus defines the observed state of DagTask
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
